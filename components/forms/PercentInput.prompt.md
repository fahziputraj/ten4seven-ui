Rates and ratios: hen-day, mortalitas, PPN, diskon.

```jsx
<PercentInput value={hd} onValueChange={setHd} />   {/* "91,8 %" */}
```

- Comma is the decimal separator — the field rejects periods on purpose.
- The `%` lives inside the field on the right, after the number.
